# Staged sidecar runtime (Node binary + bundled JS tree), produced by
# `pnpm stage:desktop:tauri` before `tauri build`. Regenerable; not committed.
node-app/

# Rust build output (the big one — thousands of compiled artifacts)
target/

# Auto-generated by `tauri dev`/`tauri build` (capability ACL schemas)
/gen/

# Cargo backup files
**/*.rs.bk
