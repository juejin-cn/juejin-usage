---
"@juejin-opensource/jusage-core": patch
---

修复 Claude Code 与 Codex 用量在同步恰好撞上日志写入中途时被永久丢失的问题：此前游标无条件推进到文件末尾，那条只写了一半的记录在写完后也不会再被读取，用量静默消失（受控复现中一条 5,800 token 的消息 100% 丢失）。现在游标只推进到最后一条完整记录，半截记录留到下一轮重读；同时修正了扫描期间文件继续增长时可能重复计入的边界。
